{
  "src": ["./gulp/tasks/**/*.js"],
  "tasks": {
    "default": {
      "deps": ["clean", "build"]
    },
    "clean": {},
    "build": {
      "deps": ["clean"],
      "src": ["./src/**/*.js"],
      "dest": "./dist/"
    },
    "watch": {
      "tasks": []
    }
  }
}
