{
  "src": ["./gulp/tasks/**/*.js"],
  "tasks": {
    "default": {
      "deps": ["clean", "build"]
    },
    "clean": {},
    "build": {
      "deps": ["clean"],
      "src": ["./tasks"],
      "dest": "./dist/"
    },
    "watch": {
      "tasks": []
    }
  }
}
